#!/bin/bash -e

ROOT=$(dirname $0)/..
cd $ROOT

echo bin/setup_base
bin/setup_base

echo bin/setup_dev
bin/setup_dev

echo bin/build_daq
bin/build_daq

if [ -z $TEST ]; then
    echo bin/install_daq
    bin/install_daq
fi

echo Done with setup_daq.

