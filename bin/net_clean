#clean setup_dot1x
sudo ovs-vsctl del-br br-dot1x
docker rm -f daq-faux-1 || true
docker rm -f daq-faux-2 || true
docker rm -f daq-ntwk || true
docker rm -f faucet_dot1x || true

#clean setup_testing
docker kill daq-faux-satlej
docker kill daq-networking-yamuna
docker kill faucet1
docker kill faucet2
