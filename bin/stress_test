#!/bin/bash -e

ROOT=$(realpath $(dirname $0)/..)

cd $ROOT

sudo rm -rf inst
mkdir -p inst

args=-a
args+=" daq_loglevel=debug"
args+=" mininet_loglevel=debug"

cmd/exrun $args 2>&1 | rotatelogs -t -e -c inst/exrun-%I.log 3600
