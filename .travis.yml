sudo: required
dist: bionic
services:
  - docker
language: python
python:
  - "3.7"
cache:
  pip: true
addons:
  apt:
    update: true
    packages:
      - openvswitch-switch
install:
  - set -e
  - bin/setup_daq
script:
  - set -e
  - bin/test_daq
cache: pip
env:
  global:
    - DOCKER_STARTUP_TIMEOUT_MS=60000
  matrix:
    - DAQ_TEST=topo TEST=1
    - DAQ_TEST=topo TEST=2
    - DAQ_TEST=topo TEST=3
    - DAQ_TEST=topo TEST=4
    - DAQ_TEST=topo TEST=5
branches:
  except:
  - /^release-\d+\.\d+(\.\d+)?$/
