{
    "base.startup.dhcp" : {
        "description" : "Check the base DHCP startup exchange"
    },
    "base.switch.ping" : {
        "description" : "Attempt to ping the OpenFlow compatible switch configured in system.conf"
    },
    "base.target.ping" : {
        "description" : "Attempt to ping the Device Under Test"
    }
}
