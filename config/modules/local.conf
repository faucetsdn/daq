# Baseline test file for officially included tests.

# Default built-in tests.
include ${DAQ_LIB}/config/modules/host.conf

# Local tests to build.
build ${DAQ_CONF}/docker

# Add the new test into the list.
add flaky

# Remove unnecessary tests.
remove nmap
remove bacnet
remove pass
remove fail
remove ping
