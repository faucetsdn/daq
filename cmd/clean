#!/bin/bash -e

ROOT=$(realpath $(dirname $0)/..)

sudo rm -rf $ROOT/inst

docker ps -aq --no-trunc | xargs -r docker rm || true

docker images -q --filter dangling=true | xargs -r docker rmi || true

docker container prune -f

