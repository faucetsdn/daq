#!/bin/bash -e

ROOT=$(realpath $(dirname $0)/..)
INSTDIR=$ROOT/inst

rm -rf $INSTDIR
mkdir -p $INSTDIR

$ROOT/cmd/cmdserver $INSTDIR < /dev/null &

CMD_PID=$!
echo Command server PID $CMD_PID

docker run --privileged -v $INSTDIR:/root/daq/inst -ti daq/runner
