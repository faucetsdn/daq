{
  "modules": {
    "ipaddr": {
      "enabled": false,
      "timeout_sec": 900,
      "port_flap_timeout_sec": 20,
      "dhcp_ranges": [{"start": "192.168.0.1", "end": "192.168.255.254", "prefix_length": 16}]
    },
    "pass": {
      "enabled": true
    },
    "nmap": {
      "timeout_sec": 600,
      "enabled": true
    },
    "switch": {
      "enabled": true,
      "manifest": {
        "connection.port_link": {
          "description": "Connect the device to the network switch. Check the device and the switch for the green connection light & no errors"
        },
        "connection.port_speed": {
          "description": "Verify the device auto-negotiates connection speed"
        },
        "connection.port_duplex": {
          "description": "Verify the device supports full duplex"
        },
        "poe.power": {
          "description": "Verify that the device draws less than the maximum power allocated by the port. This is 15.4W for 802.3af and 30W for 802.3at"
        },
        "poe.negotiation": {
          "description": "Verify the device autonegotiates power requirements"
        },
        "poe.support": {
          "description": "Verify if the device supports PoE"
        }
      }
    },
    "brute": {
      "enabled": true
    },
    "password": {
      "dictionary_dir": "resources/faux",
      "enabled": true
    },
    "bacext": {
      "enabled": true
    },
    "tls": {
      "enabled": true,
      "manifest": {
        "security.tls.v1_3": {
          "description": "Verify the device supports TLS 1.3 (as a client)"
        },
        "security.tls.v1_3.x509": {
          "description": "Verify the devices supports RFC 2459 - Internet X.509 Public Key Infrastructure Certificate and CRL Profile"
        },
        "security.tls.v1_2": {
          "description": "Verify the device supports TLS 1.2 (as a client)"
        },
        "security.tls.v1_2.x509": {
          "desc": "Verify the devices supports RFC 2459 - Internet X.509 Public Key Infrastructure Certificate and CRL Profile"
        },
        "security.tls.v1": {
          "description": "Verify the device supports TLS 1.0 (as a client)"
        },
        "security.tls.v1.x509": {
          "description": "Verify the devices supports RFC 2459 - Internet X.509 Public Key Infrastructure Certificate and CRL Profile"
        }
      }
    },
    "udmi": {
      "enabled": true
    },
    "network": {
      "enabled": true
    },
    "ping": {
      "enabled": true,
      "manifest": {
        "base.startup.dhcp": {
          "description": "Check the base DHCP startup exchange"
        },
        "base.switch.ping": {
          "description": "Attempt to ping access switch (if configured)"
        },
        "base.target.ping": {
          "description": "Attempt to ping the Device Under Test"
        }
      }
    }
  }
}
